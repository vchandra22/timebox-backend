app:
  name: boilerplate-golang
  gin_mode: debug
  host: localhost
  port: 8080
  cors_allowed_origins:
    - http://localhost:3000

database:
  pgsql:
    db_name:
      host: localhost
      port: 5432
      username: postgres
      password: postgres
      dbname: boilerplate_database

redis:
  host: localhost
  port: 6379
  username:
  password:
  dbname:
  dbindex: 0

jwt:
  secret: change-me

external:
  aws:
    region:
    access_key_id:
    secret_access_key:
    s3_bucket:
  cloudinary:
    cloud_name:
    api_key:
    api_secret:
  rest_client:
    base_url:
    api_key:
    timeout_seconds: 10
